node_modules
dist

# Personal/local development scripts (contain absolute paths to a specific vault)
test-in-obsidian.sh
deploy-all.sh
migrate-forks.sh

# Claude Code local settings (contain absolute paths + personal hooks)
.claude/